server: { port: 9002 }
spring:
  application.name: permission-service
  datasource: { url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/rbac?useSSL=false&serverTimezone=UTC, username: ${MYSQL_USER:rbac}, password: ${MYSQL_PASSWORD:rbac123}, driver-class-name: com.mysql.cj.jdbc.Driver }
