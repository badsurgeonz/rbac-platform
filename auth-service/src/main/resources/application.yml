server: { port: 9001 }
spring:
  application.name: auth-service
  data.redis: { host: ${REDIS_HOST:localhost}, port: ${REDIS_PORT:6379} }
  rabbitmq: { host: ${RABBITMQ_HOST:localhost}, port: 5672, username: guest, password: guest }
security.jwt.secret: ${JWT_SECRET:change-me-change-me-change-me-32}
